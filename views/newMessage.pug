extends layouts/main-layout.pug
    
block content
    .chatPageContainer
        .chatTitleBar
            label(for="userSearchTextBox") To: 
            #selectedUsers 
            input#userSearchTextBox(type="text" placeholder="Type the name of the person you are looking for")
        .resultsContainer
        button#createChatButton(disabled) Create Chat
